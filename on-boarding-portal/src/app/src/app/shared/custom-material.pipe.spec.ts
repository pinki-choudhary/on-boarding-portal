import { CustomMaterialPipe } from './custom-material.pipe';

describe('CustomMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
