import { CategoryWiseStudentPipe } from './category-wise-student.pipe';

describe('CategoryWiseStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryWiseStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
